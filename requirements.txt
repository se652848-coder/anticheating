# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Computer vision
opencv-python-headless>=4.9.0
mediapipe>=0.10.9,<=0.10.14

# Object detection (torch installed separately in Dockerfile for CPU-only build)
ultralytics>=8.2.0

# Utilities
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0
